# NBC Service — ระบบบริหารงานซ่อมและบริการ

ระบบบริหารงานซ่อม ล้างแอร์ และบำรุงรักษา สำหรับ **บริษัท เอ็นบีซี กรุ๊ป จำกัด**
ครอบคลุมตั้งแต่รับแจ้งงาน → จองคิวตามโควตา → จ่ายงานช่าง → ใบงานหน้างานพร้อมรูปและลายเซ็น → PDF → ประวัติ → รายงาน

ระบบนี้ทำงาน **แยกจากเว็บไซต์เดิม** [nbcgroup.co.th](https://nbcgroup.co.th) (WordPress) โดยใช้แบรนด์เดียวกัน
แต่ไม่แตะโค้ดเว็บเดิม — เว็บเดิมยังเป็นหน้าร้านสำหรับการตลาดเหมือนเดิม

> **สถานะ (3 ก.ย. 2569):** **เฟส 1 และ 2 เสร็จครบ ส่งมอบแล้ว** · **ลูกค้าใช้ระบบพนักงานจริงแล้ว** · เฟส 3 ทำไปบางส่วน
> เทสต์ผ่าน **553/553** กับ Postgres จริง · typecheck ผ่าน · `next build` ผ่าน · Lighthouse `/login` 100 · `/booking` 91
>
> 👉 **มารับงานต่อ เริ่มที่ [`docs/07-HANDOFF-2026-08-26.md`](docs/07-HANDOFF-2026-08-26.md)**
> เขียนให้คนที่ไม่เคยเห็นโปรเจกต์นี้อ่านแล้วทำงานต่อได้ · แผนงานฉบับเต็มอยู่ใน
> [`docs/03-PHASE-ROADMAP.md`](docs/03-PHASE-ROADMAP.md)
>
> 🔴 **ก่อนแตะโค้ดส่วนพนักงาน** ระบบเก็บเลขบัตรประชาชนและเลขบัญชีธนาคารแล้ว
> ต้องตั้ง `FIELD_ENCRYPTION_KEY` และอ่านกติกา 5 ข้อในเอกสารส่งมอบก่อน
>
> 🔴🔴 **ทะเบียนพนักงานบน production คือข้อมูลจริงของลูกค้าแล้ว — 21 คน มีชื่อจริง เลขบัตรจริง**
> ตั้งแต่ 2 ก.ย. 2569 เจ้าของบริษัทกรอกพนักงานเองครบทุกคน (รหัส A01 … R03)
>
> **ห้ามรัน `scripts/clean-demo-staff.mjs --apply` เด็ดขาด** — สคริปต์นั้นเขียนไว้ตอนทะเบียนยังว่าง
> มันลบพนักงาน **ทุกคน** ไม่ได้เลือกเฉพาะข้อมูลตัวอย่าง วันนี้มันจะลบทะเบียนจริงทั้งชุด
> ถ้าต้องลบแถวทดสอบ ให้ลบแบบเจาะจงทีละแถว และนับ cascade ก่อนเสมอ
>
> **ห้ามรัน seed หรือ `scripts/demo-day.mjs` ทับ** ด้วยเหตุผลเดียวกัน
> ข้อมูลฝั่งงาน (ช่าง 6 · ทีม 3 · เวร 252 · ลูกค้า 5 · เครื่อง 54 · งาน 12) ยังเป็นข้อมูลตัวอย่าง

## 🌐 ใช้งานจริง

| | |
|---|---|
| **จองคิว (ลูกค้า)** | https://nbcserv.vercel.app/booking |
| **ติดตามงาน (ลูกค้า)** | https://nbcserv.vercel.app/track |
| **เข้าระบบ (พนักงาน)** | https://nbcserv.vercel.app/login |

โฮสต์บน Vercel (`touchmecodes-projects/nbcserv`) · ฐานข้อมูล Supabase สิงคโปร์
โดเมนเป้าหมาย `app.nbcgroup.co.th` — รอสิทธิ์จัดการ DNS จากลูกค้า

> 🔴 **`vercel --prod` ขึ้น `Error: fetch failed` ไม่ได้แปลว่า deploy ล้ม**
> เจอจริงเมื่อ 2 ก.ย. — build สำเร็จทุกครั้ง (`● Ready`) แต่ขั้นสุดท้ายที่ย้ายชื่อเว็บ
> มาที่ build ใหม่ล้มเพราะเน็ตช้า **ผลคือ production ค้างอยู่ที่ของเก่า 5 ชั่วโมง
> โดยที่ commit ขึ้น git ไปแล้วเรียบร้อย** ซึ่งอ่านเผินๆ เหมือนงานเสร็จ
>
> ```bash
> npx vercel ls --prod          # ดู Age ของ production ล่าสุด — ถ้าเก่ากว่า commit แปลว่ายังไม่ขึ้น
> npx vercel alias set <deployment-url> nbcserv.vercel.app
> ```
>
> **ยืนยันทุกครั้งหลัง deploy** ว่า `npx vercel ls --prod` แสดง Age ไม่กี่นาที
> อย่าเชื่อข้อความบนจอ ให้เชื่อ Age

> ⚠️ **push ขึ้น git แล้ว Vercel ไม่ deploy ให้อัตโนมัติ** — Vercel เชื่อมกับ repo นี้ไม่ได้
> (repo อยู่บัญชี `WitoonNon` คนละบัญชีกับ Vercel) การขึ้น production ทำผ่าน CLI เท่านั้น
> **ทีมงาน: แก้เสร็จ → เปิดดูที่ `localhost` → เทสต์ผ่าน → push → แจ้งเจ้าของโปรเจกต์ deploy รวดเดียว**

---

## สรุปสั้น ทำถึงไหนแล้ว

| ส่วน | สถานะ |
|---|---|
| ฐานข้อมูล 52 ตาราง 27 enum + migration 3 ชุด | ✅ ใช้งานจริงบน Supabase |
| ระบบโควตารายวัน (3 แกน + กันจองซ้ำ) | ✅ ทดสอบ concurrency ผ่าน |
| ค่าเข้าตรวจเช็ค + หักคืนเป็นส่วนลด | ✅ |
| ระบบล็อกอิน + สิทธิ์ตามบทบาท | ✅ ทดสอบ 11 กรณีผ่าน |
| รับแจ้งงาน / รายการงาน / รายละเอียดงาน | ✅ |
| กระดานจ่ายงาน + มาตรวัดกำลังทีม | ✅ |
| ปฏิทินโควตา + เปิด-ปิดวัน | ✅ |
| ทะเบียนลูกค้า + Customer 360 | ✅ |
| ตั้งค่าระบบ (ผู้ใช้ ราคา ค่าธรรมเนียม ฟอร์ม) | ✅ |
| ตั้งค่าโควตารายวัน (3 แกน) จากหน้าเว็บ | ✅ `/settings/quota` + ปรับรายวันที่ `/schedule` |
| ใบซ่อม ตรงตามฟอร์มจริงของบริษัท | ✅ v2 เผยแพร่ใน DB แล้ว |
| หน้าจองคิวฝั่งลูกค้า | ✅ จองจบจริง ตัดโควตาจริง |
| หน้าติดตามงานสำหรับลูกค้า `/track` | ✅ |
| งานเบื้องหลัง (กวาดคิวค้าง · ต่ออายุปฏิทิน) | ✅ Vercel Cron |
| บังคับเปลี่ยนรหัสผ่านครั้งแรก | ✅ |
| **บันทึกใบงาน (ร่าง → ส่ง → อนุมัติ/ตีกลับ)** | ✅ `/work-orders/d/[id]` — Phase 2.1 |
| **แนบรูปถ่ายหน้างาน** | ✅ Supabase Storage (bucket private + signed URL) — Phase 2.2 |
| **ลายเซ็นบนมือถือ + ผูกกับ SHA-256 ของเนื้อหา** | ✅ ตรวจได้ว่าเอกสารถูกแก้หลังเซ็นหรือไม่ — Phase 2.3 |
| **แอปช่าง: เปลี่ยนสถานะจากหน้างาน + ทำงานออฟไลน์** | ✅ คิวส่งภายหลัง — Phase 2.5 |
| **คิวใบงานรอตรวจสำหรับหัวหน้างาน** | ✅ `/work-orders` แยกตามสถานะ + เตือนเมื่อฟอร์มถูกแก้หลังเซ็น |
| **เปิด/ปิดการเก็บเวลาถ่ายรูปและพิกัด GPS** | ✅ `/settings/capture` — พิกัดปิดไว้ตามค่าเริ่มต้น (PDPA) |
| **แนบรูปย้อนหลังหลังส่งใบงาน** | ✅ ต้องมีสิทธิ์ตรวจใบงาน + ระบุเหตุผล · แสดงแยกจากฟอร์มที่เซ็นไปแล้ว |
| **Export PDF ภาษาไทย** | ✅ พิมพ์จากเบราว์เซอร์ · หน้า A4 จริง — Phase 2.4 |
| **แจ้งเตือน LINE** | ✅ ส่งเข้าไลน์จริงแล้ว · ผูกบัญชีลูกค้าผ่าน LINE Login — Phase 2.6 |
| **ทะเบียนเครื่องปรับอากาศ** | ✅ `/assets` · ประวัติผูกกับตัวเครื่อง บอกได้ว่าเครื่องไหนซ่อมบ่อยจนควรเปลี่ยน — Phase 3.1 |
| **กราฟบนหน้าภาพรวม** | ✅ งาน 12 เดือน · คิว 14 วัน · งานค้าง — เอนจินกราฟไม่อยู่ใน critical path |
| **แบ่งหน้า (pagination)** | ✅ แทนที่เพดาน `take: 300` ที่เคยตัดข้อมูลทิ้งเงียบๆ |
| **ทะเบียนประวัติพนักงาน** | ✅ `/employees` · เลขบัตร ปชช./เลขบัญชี เข้ารหัส AES-256-GCM + บันทึกว่าใครเปิดดู |
| **ประวัติการปรับค่าแรง** | ✅ ค่าแรงที่มีผล ณ วันใดวันหนึ่ง — เงื่อนไขที่ทำให้คำนวณเงินเดือนย้อนหลังได้ถูก |
| **ชั้น raw SQL** | ✅ `src/lib/db/sql.ts` — ปิดกับดัก BigInt · เงื่อนไข optional · query ที่ช้าโดยไม่มีใครเห็น |
| รายงานและสถิติฉบับเต็ม | ⬜ Phase 3.2 |
| เชื่อมกับเว็บเดิม nbcgroup.co.th | ⬜ Phase 3.3 |
| นัด PM อัตโนมัติ | 🟡 Phase 3.4 — เขียนแล้ว เทสต์ผ่าน 33 ข้อ **ยังไม่ได้ตรวจกับ DB จริง** |
| แอดมินแก้ไข/ลบรูป | ⬜ Phase 3.6 — ลูกค้าขอ 25 ส.ค. 2569 |
| **ลงเวลา QR+GPS · โอที · ลา · เงินเดือน** | ✅ **ลูกค้าใช้จริงแล้ว** — ใบเสนอราคาแยก 10,000 · พนักงาน 21 คนในระบบ |
| **บัญชีเข้าระบบของพนักงาน** | ✅ สร้างจากหน้าพนักงาน · บังคับตั้งรหัสใหม่ครั้งแรก |
| **QR ลงเวลา 2 แบบ** | ✅ พิมพ์ติดผนัง (ถาวร) และแสดงบนจอ (30 วินาที) · เข้าหน้าสแกนเดียวกัน |
| อัปโหลดเอกสารพนักงาน (สำเนาบัตร/ทะเบียนบ้าน/วุฒิ) | 🔴 ลูกค้าขอ 26 ส.ค. · **อยู่ในรายการ "ไม่รวม" ยังไม่ตกลงราคา** |
| ระบบสต๊อก/อะไหล่/เบิกจ่าย/เครื่องมือช่าง | 🟢 **ตกลงแล้ว 3 ก.ย. 2569** · เริ่มหลังเฟส 3 — ขอบเขตอยู่ใน [docs/11](docs/11-STOCK-SYSTEM-AGREED-2026-09-03.md) |
| สลิปเงินเดือน PDF | 🔴 ลูกค้าขอ 25 ส.ค. · **อยู่ในรายการ "ไม่รวม" ของใบ 10,000 ยังไม่ตกลงราคา** |

---

## เริ่มใช้งาน

```bash
npm install
cp .env.example .env          # แล้วใส่ DATABASE_URL
npx prisma generate
npx prisma migrate deploy
npm run db:seed
npm test                      # ต้องได้ 178/178 ก่อนเริ่มแก้อะไร
npm run dev
```

เปิด http://localhost:3000

### ไฟล์แนบ (รูปถ่าย · ลายเซ็น)

บน production ใช้ **Supabase Storage** — `.env` ต้องมี:

```
STORAGE_DRIVER="supabase"
SUPABASE_URL="https://<ref>.supabase.co"
SUPABASE_SERVICE_ROLE_KEY="..."      # ห้ามมี NEXT_PUBLIC_ นำหน้า
SUPABASE_STORAGE_BUCKET="work-orders"
```

> ⚠️ **bucket ต้องเป็น private** — key ของไฟล์เดาได้โดยออกแบบ (`yyyymm/entity/id/kind/file`)
> ถ้าเปิด public = ใครก็เปิดดูรูปในบ้านลูกค้าได้ ระบบเสิร์ฟผ่าน signed URL เท่านั้น
>
> ⚠️ **ห้ามใช้ `STORAGE_DRIVER=local` บน Vercel** — ดิสก์เป็น ephemeral รูปที่อัปจะหายทันที
> ในเครื่องตัวเองใช้ `local` ได้ปกติ
>
> ถ้าตั้ง `supabase` แต่ไม่ใส่ URL/KEY **แอปจะไม่ start เลย** ตั้งใจให้ fail ตอน boot
> ไม่ใช่ตอนช่างกดอัปโหลดอยู่บนหลังคา

> ⚠️ **DATABASE_URL ต้องเป็น pooler ไม่ใช่ direct host**
> `db.<ref>.supabase.co` เป็น **IPv6-only** — บางเครือข่าย resolve ไม่ได้เลย
> (อาการคือ `getaddrinfo ENOENT` หรือ `P1001` แบบสุ่ม) และ Vercel ต่อไม่ได้แน่นอน
>
> ```
> aws-0-ap-southeast-1.pooler.supabase.com:5432   ← session mode: dev / migrate / test
> aws-0-ap-southeast-1.pooler.supabase.com:6543   ← transaction mode: บน Vercel
> ```

### บัญชีสำหรับพัฒนา — รหัสเดียวกันหมด `nbc-dev-1234`

> 🔴 **`admin@nbcgroup.co.th` เป็นบัญชีของลูกค้า — รหัสไม่ได้อยู่ในเอกสารนี้**
> เปลี่ยนเป็นรหัสจริงเมื่อ 3 ก.ย. 2569 ก่อนส่งมอบ ขอจากเจ้าของโปรเจกต์
> บัญชีอื่นที่เหลือยังเป็น `nbc-dev-1234` เพราะเป็นบัญชีตัวอย่างสำหรับพัฒนาเท่านั้น

| อีเมล | บทบาท | เห็นอะไร |
|---|---|---|
| `admin@nbcgroup.co.th` | ผู้ดูแลระบบสูงสุด | ทุกเมนู — **รหัสจริงของลูกค้า ไม่ได้เขียนไว้ที่ไหน** |
| `office@nbcgroup.co.th` | ธุรการ | รับแจ้งงาน ลูกค้า ไม่มีตั้งค่า |
| `dispatch@nbcgroup.co.th` | ผู้จ่ายงาน | จ่ายงาน โควตา ไม่มีตั้งค่า |
| `supervisor@nbcgroup.co.th` | หัวหน้างาน | อนุมัติใบงานและใบเสนอราคา |
| `tech-001@` … `tech-006@` | ช่าง | เข้าแอปช่างอัตโนมัติ เห็นเฉพาะงานทีมตัวเอง |

### คำสั่งที่ใช้บ่อย

| คำสั่ง | ทำอะไร |
|---|---|
| `npm run db:check` | เช็คว่าต่อ DB ได้ และรองรับ `SELECT FOR UPDATE` |
| `npm run db:seed` | ใส่ข้อมูลตั้งต้น (รันซ้ำได้ ไม่พัง) |
| `node scripts/demo-day.mjs` | สร้างงานตัวอย่างของวันนี้ สำหรับเดโม |
| `npm test` | เทสต์ทั้งหมด 553 ข้อ (ต้องมี DB · ใช้เวลา ~12 นาที) |
| `npm run typecheck` | ตรวจ TypeScript ทั้งโปรเจกต์ |
| `npm run client-confirm` | ดูรายการค่าที่ยังรอลูกค้ายืนยัน |

### ชุดเทสต์

**36 ไฟล์ · 553 ข้อ** เรียงตามกลุ่ม (นับจากการรันจริง 3 ก.ย. 2569)

#### ฝั่งงานซ่อม — 180 ข้อ

| ไฟล์ | ข้อ | DB | ป้องกันอะไร |
|---|---|---|---|
| `quota.concurrency` | 7 | ✅ | ขายคิวเกิน — ล็อกแถวจริง mock ไม่ได้ |
| `booking.quota` | 8 | ✅ | จองผ่านเว็บต้องตัดโควตา |
| `workorder.lifecycle` | 13 | ✅ | ใบงานที่อนุมัติแล้วต้องแก้ไม่ได้ |
| `workorder.signature` | 15 | ✅ | ลายเซ็นต้องผูกกับเนื้อหาที่เซ็นจริง |
| `workorder.access` | 20 | ✅ | ใบงานต้องเห็นได้เฉพาะคนที่เกี่ยวข้อง |
| `media.attachment` | 18 | ✅ | อัปซ้ำต้องไม่ทับหลักฐาน · แนบย้อนหลังต้องมีเหตุผล |
| `media.capture-policy` | 9 | ✅ | ปิดเก็บพิกัดแล้วต้องไม่เก็บจริง |
| `jobs.fieldwork` | 14 | ✅ | ช่างเปลี่ยนสถานะได้เฉพาะงานทีมตัวเอง |
| `catalog.pricing` | 11 | ✅ | ราคาที่ลูกค้ายืนยันแล้วต้องไม่เพี้ยน |
| `assets.register` | 10 | ✅ | ทะเบียนเครื่อง · แบ่งหน้า · ธงเตือนซ่อมบ่อย |
| `scheduling.pm` | 13 | ✅ | นัด PM อัตโนมัติ (เพื่อนร่วมทีมเขียน) |
| `scheduling.pm-planner` | 20 | ❌ | กระจายงานไม่ให้กระจุกวันเดียว |
| `reports.dashboard` | 13 | ✅ | ตัวเลขบนหน้าภาพรวมและกราฟ |
| `platform.config` | 13 | ✅ | แก้ค่าตั้งค่าระบบแล้วต้องมี audit log |
| `db.sql` | 17 | ✅ | raw SQL helper · BigInt · การเตือน query ช้า |

#### ฝั่งพนักงาน (HR) — 240 ข้อ

| ไฟล์ | ข้อ | DB | ป้องกันอะไร |
|---|---|---|---|
| `hr.employee` | 35 | ✅ | เข้ารหัสเลขบัตร · **แก้ไขแล้วบัญชีต้องไม่หลุด** · เรียงตามรหัส · อักขระแฝง |
| `hr.employee-login` | 15 | ✅ | สร้าง/รีเซ็ต/ยกเลิกบัญชีพนักงาน |
| `hr.timeclock` | 25 | ✅ | ตอกบัตร · **ทดลองงานต้องลงเวลาได้** · สถานะที่ต้องปฏิเสธ |
| `hr.timeclock-token` | 18 | ❌ | QR ถาวรและ QR 30 วินาที |
| `hr.geofence` | 17 | ❌ | ระยะห่างจากจุดสแกน · ความแม่นยำที่เชื่อไม่ได้ |
| `hr.worktime` | 17 | ❌ | จับคู่เข้า-ออก · ลืมกดออก · ข้ามเที่ยงคืน · วันแบบไทย |
| `hr.payroll` | 24 | ✅ | คำนวณเงินเดือน · ปิดงวด · สลิป |
| `hr.payroll-rules` | 34 | ❌ | อัตราขั้นต่ำตามกฎหมาย · **ขึ้นค่าแรงกลางงวดต้องคิดแยกช่วง** |
| `hr.wage` | 21 | ✅ | ประวัติค่าแรงรายคน · ย้อนหลังไปวันเริ่มงาน |
| `hr.leave-rules` | 19 | ❌ | สิทธิ์ลาและการหักเงิน |
| `hr.self-service` | 15 | ✅ | หน้าของพนักงานเห็นเฉพาะของตัวเอง |

#### ฟอร์ม · ออฟไลน์ · แจ้งเตือน — 133 ข้อ

| ไฟล์ | ข้อ | DB | ป้องกันอะไร |
|---|---|---|---|
| `forms.validator` | 17 | ❌ | ฟอร์มที่กรอกถูกต้องต้องส่งผ่าน |
| `forms.print` | 13 | ❌ | เลย์เอาต์ A4 · ฟอนต์ไทยฝังครบ |
| `forms.renderer` | 5 | ❌ | การผูก state ของฟอร์ม |
| `offline.outbox` | 13 | ❌ | **งานที่คิวไว้ต้องไม่หายเงียบ** |
| `offline.contracts` | 12 | ❌ | ชื่อไฟล์/hash ฝั่ง browser ต้องตรงกับ server |
| `notify.line` | 13 | ❌ | ข้อความ LINE |
| `notify.job` | 10 | ✅ | ส่งแจ้งเตือนตอนสถานะงานเปลี่ยน |
| `notify.line-login` | 10 | ❌ | ล็อกอินผ่าน LINE |
| `media.exif` | 10 | ❌ | อ่าน/ลบ EXIF จากรูปถ่าย |
| `storage.supabase` | 9 | ❌ | storage adapter |

---

## สถาปัตยกรรม

| ชั้น | เทคโนโลยี | เหตุผล |
|---|---|---|
| หน้าเว็บ | Next.js 16 (App Router), React 19, TypeScript | โค้ดชุดเดียวรองรับ 3 หน้าจอ |
| ดีไซน์ | Tailwind v4 + โทเคนสีจากเว็บบริษัท | สีส้ม `#E4750E` น้ำเงิน `#2891BD` กรมท่า `#132945` |
| ฟอนต์ | Mitr (หัวข้อ) + Sarabun (เนื้อหา) **โฮสต์เอง** | แอปช่างต้องทำงานตอนไม่มีเน็ต |
| ฐานข้อมูล | PostgreSQL 17 (Supabase, สิงคโปร์) + Prisma 7 | ล็อกแถวได้จริง เก็บฟอร์มเป็น JSONB |
| ล็อกอิน | Session ในฐานข้อมูล + cookie httpOnly | เพิกถอนสิทธิ์ได้ทันที |
| ไฟล์แนบ | `StorageAdapter` (local ⇄ S3/R2) | ยังไม่ต้องตัดสินใจเรื่อง hosting |
| แจ้งเตือน | `NotificationChannel` (console ⇄ LINE ⇄ email) | ยังไม่มีสิทธิ์ LINE ก็พัฒนาต่อได้ |

### 3 หน้าจอ

- `src/app/(staff)` — ออฟฟิศ ธุรการ ผู้จ่ายงาน หัวหน้างาน (จอคอมพิวเตอร์)
- `src/app/(tech)` — ช่างภาคสนาม (มือถือ ปุ่มใหญ่ ออกแบบเผื่อไม่มีสัญญาณ)
- `src/app/(portal)` — ลูกค้า จองคิวและติดตามงาน

### โครงสร้างโมดูล

`prisma/schema/*.prisma` กับ `src/modules/*` จับคู่กัน 1:1

```
identity  customers  contracts  catalog  scheduling  jobs
dispatch  workorders billing    media    notifications platform
```

---

## 3 จุดที่ควรอ่านก่อน

### 1. ระบบโควตา — `src/modules/scheduling/quota.service.ts`

คุมกำลังรับงาน **3 แกนพร้อมกัน**: จำนวนงาน · จำนวนเครื่อง · **นาทีทีมช่าง**

แกนที่สามสำคัญที่สุด เพราะเว็บบริษัทประกาศเวลาทำงานต่อเครื่องไว้ 30/40/60/90 นาที
การกำหนดว่า "วันละ 10 งาน" จึงไม่มีความหมาย — งานล้างแอร์ติดผนัง 10 เครื่อง กับงานแอร์ซ่อนในฝ้า 10 เครื่อง
กินกำลังช่างต่างกันสามเท่า

**หนึ่งช่องต่อ (วัน × เขต × ประเภทงาน)** ไม่แยกตามขนาดงาน เพราะช่างทีมเดียวกันทำทั้งงานเล็กงานใหญ่
ถ้าแยกช่องตามขนาด วันหนึ่งอาจขึ้นว่า "ยังว่าง" สำหรับงานเล็ก ทั้งที่ช่างถูกจองหมดแล้ว

การจองใช้ `SELECT ... FOR UPDATE` ล็อกแถวเดียว ลูกค้าสองคนแย่งคิวสุดท้ายพร้อมกันจะสำเร็จแค่คนเดียว
และมี `CHECK constraint` ในฐานข้อมูลเป็นด่านสอง — ต่อให้โค้ดมีบั๊ก Postgres ก็ไม่ยอมให้รับงานเกิน

### 2. ค่าเข้าตรวจเช็ค — `src/modules/billing/fee.service.ts`

เก็บเป็น **บัญชีเดินสะพัด เขียนเพิ่มอย่างเดียว** ไม่ใช่ตัวเลขที่แก้ทับได้

| เหตุการณ์ | สิ่งที่บันทึก |
|---|---|
| รับงาน ลูกค้าทั่วไป | `+ค่าตรวจเช็ค` |
| รับงาน ลูกค้าในสัญญา | **ไม่บันทึกเลย** ระบุเหตุผลว่ายกเว้นตามสัญญา |
| ลูกค้าไม่ซ่อมต่อ | ค่าตรวจเช็คคงอยู่ ออกบิลตามนั้น |
| ลูกค้าตกลงซ่อม | `+ส่วนลด` (ค่าติดลบ) |

ยอดสุทธิคำนวณจาก `SUM(amountSigned)` เสมอ — ตรวจย้อนหลังได้ และไม่มีทางผิดเพราะมีคนแก้ตัวเลข

### 3. แบบฟอร์มใบงาน — `src/lib/forms/`

ฟอร์มทั้ง 3 ใบเก็บเป็น **ข้อมูลในฐานข้อมูล ไม่ใช่โค้ด** เมื่อลูกค้าส่งฟอร์มจริงมาก็เพิ่มเป็นเวอร์ชันใหม่ได้ทันที

`templateVersion` ถูกบันทึกไว้บนใบงานแต่ละใบ — ใบงานที่ออกปี 2569 จะยังแสดงผลแบบเดิมเสมอ
แม้ฟอร์มจะเปลี่ยนไปแล้วในปี 2570

`Signature.payloadHash` เก็บ SHA-256 ของเนื้อหาตอนที่เซ็น ถ้ามีการแก้ไขภายหลังจะตรวจจับได้
— นี่คือความต่างระหว่าง "รูปลายเซ็น" กับ "หลักฐาน"

---

## ข้อมูลตั้งต้นเป็นข้อมูลจริง

`prisma/seed/03-catalog.ts` คือ **ตารางราคาที่บริษัทประกาศบนเว็บไซต์จริง** — ราคา 2 ระดับ
(ลูกค้าในสัญญา / ลูกค้าทั่วไป) ครบทุกประเภทเครื่องและช่วง BTU พร้อมเวลาทำงานมาตรฐาน
รวมถึงรอบ PM 2/3/4 ครั้งต่อปีตามลักษณะการใช้งาน

ระบบจึงเดโมให้ลูกค้าดูได้ตั้งแต่วันแรก และตัวเลขที่เห็นเป็นตัวเลขที่ลูกค้าคุ้นเคย

---

## ยังขาดอะไร

> 📋 **แผนงานฉบับเต็มเรียงลำดับพร้อมเกณฑ์ว่าเสร็จ อยู่ที่ [`docs/03-PHASE-ROADMAP.md`](docs/03-PHASE-ROADMAP.md)**
> หัวข้อข้างล่างเป็นสรุปย่อ

### 🔴 ต้องทำก่อน / กำลังบล็อกอยู่

1. **แยกฐานข้อมูลสำหรับเทสต์** — `npm test` ใช้ `DATABASE_URL` ตัวเดียวกับ production
   ซึ่งตอนนี้มีข้อมูลจริงของลูกค้าอยู่ อ่านรายละเอียดที่หัวข้อ [การทดสอบ](#การทดสอบ)
2. **รหัสฐานข้อมูล Supabase** — รหัสปัจจุบันเคยส่งผ่านแชท ยังไม่ได้เปลี่ยน
   ต้องทำที่ **Supabase → Settings → Database → Reset database password** เท่านั้น
   เปลี่ยนผ่าน SQL ไม่ได้ Supabase บล็อกด้วย `supautils`
   (*Only superusers can alter privileged roles* · role `postgres` มี `rolsuper: false`)
   แล้วอัปเดต `DATABASE_URL` บน Vercel ทั้ง production และ preview
3. **`npm run lint` ใช้ไม่ได้** — สคริปต์ยังเรียก `next lint` ซึ่งถูกถอดออกใน Next 16
   ต้องย้ายไป ESLint CLI

### 🟡 เฟส 3 — เหลืออยู่

| | สถานะ |
|---|---|
| **3.2 รายงานและสถิติ** | ⬜ `/reports` ยังเป็นหน้า `ComingSoon` |
| **3.3 เชื่อมกับ nbcgroup.co.th** | ⬜ ยังไม่เริ่ม · รอสิทธิ์จัดการ DNS |
| **3.4 นัด PM อัตโนมัติ** | 🟡 เขียนแล้ว เทสต์ผ่าน 33 ข้อ **ยังไม่ได้ตรวจกับ DB จริง** — งานที่เร็วที่สุดที่เหลือ |
| **3.6 แอดมินแก้ไข/ลบรูป** | ⬜ ยังไม่เริ่ม |
| **QR ติดหน้าเครื่อง** | ⬜ สแกนแล้วเห็นประวัติเครื่องนั้น |

### 🟢 ทำเสร็จแล้ว (เคยอยู่ในรายการนี้)

หน้าตั้งค่าโควตา `/settings/quota` · จองคิวหน้าเว็บจบจริง · `/track` ติดตามงาน ·
ใบงานหน้างานครบชุด (บันทึก · รูป · ลายเซ็น · PDF · ออฟไลน์) · แจ้งเตือน LINE ส่งจริงแล้ว ·
ทะเบียนเครื่อง `/assets` · กราฟหน้าภาพรวม · บังคับเปลี่ยนรหัสครั้งแรก ·
ระบบลงเวลา/โอที/ลา/เงินเดือน (ลูกค้าใช้จริงแล้ว)

### 🟡 ควรมีก่อนขยายการใช้งาน

- **ย้ายตัวจำกัดการล็อกอินผิดไป Redis** — ตอนนี้เก็บใน memory หายเมื่อรีสตาร์ท
- **สำรองข้อมูล + Monitoring** — Supabase แผนฟรีไม่มี backup อัตโนมัติ
  ตอนนี้มีข้อมูลจริงของลูกค้าแล้ว ข้อนี้สำคัญขึ้นกว่าเดิมมาก
- **SUPER_ADMIN สำรอง** — ตอนนี้มีบัญชีเดียวคือ `admin@nbcgroup.co.th` ซึ่งเป็นของลูกค้า

### ⏳ รอลูกค้า (ไม่บล็อกงานข้างบน)

- **ฟอร์มจริงอีก 2 ใบ** — ใบตรวจเช็ค/แจ้งซ่อม และใบล้าง/PM (ได้ใบซ่อมมาแล้ว)
- **เกณฑ์ส่วนลดตามจำนวนเครื่อง** — ลูกค้ายืนยันว่า "จำนวนเยอะราคาต่อเครื่องจะต่ำลง"
  แต่ยังไม่ให้ขั้นบันได ระบบจึงแสดงเป็นช่วงราคาให้เจ้าหน้าที่ระบุราคาจริง (`@client-confirm D6`)
- **สิทธิ์จัดการ DNS** — สำหรับโดเมน `app.nbcgroup.co.th`
- **วันหยุดนักขัตฤกษ์แบบจันทรคติ** (มาฆบูชา วิสาขบูชา อาสาฬหบูชา เข้าพรรษา)
  ระบบใส่เฉพาะวันที่ตายตัว ไม่เดาวันจันทรคติ เพราะใส่ผิดจะปิดรับงานในวันทำงานโดยไม่มีใครรู้
- ตารางราคาฉบับที่ใช้จริงภายใน · รายการอะไหล่จริง

### 💰 งานที่ยังไม่ตกลงราคา — อย่าเริ่มทำ

| | หมายเหตุ |
|---|---|
| สลิปเงินเดือน PDF | อยู่ในรายการ "ไม่รวมในรอบนี้" ของใบเสนอราคา QT-2024-052 |
| แนบสำเนาเอกสารในทะเบียนประวัติ | เช่นเดียวกัน — เขียนชื่อไว้ตรงตัวทั้งคู่ |
| **ระบบสต๊อก/อะไหล่/เบิกจ่าย** | ลูกค้าขอ 3 ก.ย. 2569 · นอกทุกใบเสนอราคา · รอคำตอบ 3 คำถาม |

รายละเอียดและคำถามที่ส่งถามลูกค้าอยู่ใน [`docs/03-PHASE-ROADMAP.md`](docs/03-PHASE-ROADMAP.md)

---

## การทดสอบ

```bash
npm test
```

**553 เทสต์ ผ่านทั้งหมด** ส่วนใหญ่รันกับ PostgreSQL จริง ไม่ใช่ mock
เพราะสิ่งที่ทดสอบคือการล็อกแถวของฐานข้อมูล ซึ่ง mock ไม่ได้

### 🔴 เทสต์รันกับฐานข้อมูลเดียวกับที่ลูกค้าใช้จริง

ไม่มี `TEST_DATABASE_URL` แยก — `npm test` ต่อ `DATABASE_URL` ตัวเดียวกับ production
ตอนทะเบียนพนักงานยังว่างมันไม่มีอะไรเสียหาย **ตั้งแต่ 2 ก.ย. ที่ลูกค้ากรอกข้อมูลจริง มันมี**

ทุกคำสั่งลบในเทสต์ถูกกันด้วย prefix แล้ว และตรวจแล้วว่าข้อมูลลูกค้ารอดครบหลังรันชุดเต็ม

| ไฟล์ | prefix ที่ใช้กัน |
|---|---|
| `hr.employee.test.ts` | `TESTEMP-` · `testemp.` |
| `hr.employee-login.test.ts` | `TESTLOGIN-` · `testlogin.` |
| `hr.timeclock.test.ts` | `EMP-TC-` |
| `hr.payroll.test.ts` | `EMP-PAY-` · งวด `2400-` |
| `hr.wage.test.ts` | `TESTWAGE-` |
| `hr.self-service.test.ts` | `CODE_PREFIX` |

**บทเรียนที่ต้องอ่าน** — เดิม `hr.payroll.test.ts` ล้างงวดด้วย `startsWith: '2569-'`
ซึ่งเป็น **ปี พ.ศ. ปัจจุบัน** ไม่ใช่ prefix ของเทสต์ และ `openPeriod()` คืนงวดเดิมถ้ารหัสซ้ำ
แทนที่จะสร้างใหม่ ผลคือทันทีที่ลูกค้าปิดงวด `2569-08` เทสต์จะหยิบงวดจริงมาเขียนทับ
แล้วลบทิ้งตอน cleanUp **ยังไม่เคยระเบิดเพราะยังไม่มีงวดในระบบเลย**
แก้แล้วโดยย้ายไป `2400-` (พ.ศ. ที่ไม่มีวันชนของจริง) วันที่ที่ใช้คำนวณยังเป็นของเดิม

> **prefix ที่ดูเหมือนกันเทสต์ อาจไม่ได้กันอะไรเลย** ถ้าเพิ่มเทสต์ที่ลบข้อมูล
> ให้ถามก่อนว่า prefix นี้มีโอกาสตรงกับข้อมูลจริงของบริษัทไหม

### อย่ารัน vitest ซ้อนกัน

สองรอบพร้อมกันบนฐานเดียวกัน `cleanUp` ของตัวหนึ่งจะลบแถวที่อีกตัวกำลังใช้
อาการคือ `Foreign key constraint violated` ในเทสต์ที่ไม่เกี่ยวอะไรด้วยเลย
เจอมาแล้ว 2 รอบ เสียเวลาไล่หาสาเหตุนาน — **ไม่ใช่บั๊กในโค้ด**

ข้อที่สำคัญที่สุด: **จอง 20 รายการพร้อมกันแย่งคิวสุดท้าย ต้องสำเร็จ 1 รายการเท่านั้น**
การขายคิวเกินคือความผิดพลาดที่ทำให้ระบบนี้เสียหายที่สุด

### เขียนเทสต์ฟอร์มต้องระวัง

`FormRenderer` เขียนลง payload **เฉพาะคีย์ที่ผู้ใช้แตะ** — หมวดที่ช่างไม่เปิดจะไม่มีอยู่เลย
ไม่ใช่มีแล้วว่าง เทสต์รุ่นแรกใส่ `{}` ให้ทุกหมวดเอง จึงมองไม่เห็นบั๊กที่ทำให้
ฟอร์มที่กรอกถูกต้องส่งไม่ผ่าน **สร้าง payload ให้เหมือนที่ renderer ส่งจริงเสมอ**

---

## เอกสารประกอบ

| ไฟล์ | เนื้อหา |
|---|---|
| `docs/00-SYSTEM-BLUEPRINT.md` | วิเคราะห์ธุรกิจ สถาปัตยกรรม เหตุผลการออกแบบฐานข้อมูล |
| `docs/01-BUILD-PLAN-SKELETON.md` | แผนการพัฒนาและทะเบียนค่าสมมติ |
| `docs/02-CLIENT-DATA-CHECKLIST.md` | แบบสอบถามข้อมูลจากลูกค้า (ไทย-อังกฤษ) |
| **`docs/06-SPEC-LINE-NOTIFY.md`** | **สเปคงานแจ้งเตือน LINE (2.6) สำหรับคนที่มารับงานต่อ** |
| **`docs/07-HANDOFF-2026-08-26.md`** | **ส่งมอบงานให้ทีมพัฒนา — เริ่มอ่านที่ไฟล์นี้** |
| **`docs/03-PHASE-ROADMAP.md`** | **แผนงานเฟส 1·2·3 + งานเพิ่มเติม เรียงลำดับพร้อมเกณฑ์ว่าเสร็จ** |
| `docs/08-SESSION-STATE-2026-08-31.md` · `09-OPEN-DECISIONS` · `10-EMPLOYEE-SYSTEM-GAPS` | สถานะโค้ด · การตัดสินใจที่ค้าง · ช่องว่างระบบพนักงาน |
| **`docs/11-STOCK-SYSTEM-AGREED-2026-09-03.md`** | **ระบบสต๊อก — ขอบเขตที่ตกลงแล้ว ราคา และคำตอบของลูกค้าทั้งสามข้อ** |
| `docs/NBC-REQ-001-Client-Data-Checklist-TH.pdf` | แบบสอบถามข้อมูล ฉบับ PDF สำหรับส่งลูกค้า |
| **`docs/NBC-QT-2024-053-Quotation-Form-TH.pdf`** · `.html` | **ใบเสนอราคาระบบสต๊อกฉบับจริง** ตามแบบฟอร์มบริษัท — ฉบับที่ส่งลูกค้า |
| `docs/NBC-STOCK-SCOPE-REFERENCE-TH.pdf` · `.html` | เอกสารอ้างอิงภายใน **ห้ามส่งลูกค้า** — ราคาแยกรายส่วนและเมนูฟังก์ชันเสริม |

---

## ข้อควรรู้สำหรับผู้พัฒนาต่อ

- **ค่าที่ลูกค้ายังไม่ยืนยัน เก็บเป็นข้อมูลในฐานข้อมูล ไม่ฝังในโค้ด** — ดูตาราง `AppConfig`
  และหน้า `/settings/assumptions` แก้ได้โดยไม่ต้องแก้โปรแกรม
- **ชื่อตารางเป็น snake_case แต่ชื่อคอลัมน์เป็น camelCase** (ค่าเริ่มต้นของ Prisma)
  เวลาเขียน SQL ดิบต้องใส่ `"เครื่องหมายคำพูด"` ครอบชื่อคอลัมน์เสมอ
- **คอลัมน์ `@db.Date` เก็บเป็นเที่ยงคืน UTC** ต้องใช้ `dateOnly()` เทียบเสมอ
  ถ้าใช้เที่ยงคืนเวลาไทยจะเลื่อนไปวันก่อนหน้าและหาไม่เจอ (เคยเป็นบั๊กจริงในแอปช่าง)
- **`middleware.ts` รันบน Edge runtime** ห้าม import อะไรที่ใช้ `node:crypto` หรือ Prisma
  ค่าคงที่ที่ใช้ร่วมกันอยู่ใน `src/lib/auth/constants.ts`
- **CSS ที่ต้องชนะ utility ของ Tailwind ต้องอยู่นอก `@layer`** เช่นกฎขอบแดงของฟอร์ม
- **แบบฟอร์มเป็นข้อมูล ไม่ใช่โค้ด** — แก้ `src/lib/forms/templates.ts` แล้วต้องดันเข้า DB ด้วย
  ไม่งั้นฐานข้อมูลยังเป็นเวอร์ชันเดิม (เคยเป็นบั๊กจริง: โค้ดมีใบซ่อม v2 แต่ DB มีแค่ v1
  ใบงานที่ช่างกรอกจะได้ฟอร์มร่างแทนฟอร์มบริษัท)

  🔴 **แต่ห้ามใช้ `npm run db:seed` กับ production อีกแล้ว** — `seedEmployees()` สร้าง
  ทะเบียนพนักงานจากบัญชีผู้ใช้ทุกคนที่ยังใช้งานอยู่ วันนี้มันจะ **เพิ่มพนักงานตัวอย่าง
  ประมาณ 10 คน** (admin · office · dispatch · supervisor · tech-001…006) เข้าไปปนกับ
  พนักงานจริง 21 คนของลูกค้า มันไม่ลบของเดิม แต่ทะเบียนจะเปื้อนและต้องมาไล่ลบทีละคน

  ให้รันเฉพาะขั้นที่ต้องการแทน:

  ```bash
  npx tsx -e "import('./prisma/seed/06-forms.js').then(m => m.seedForms())"
  ```

  ขั้นที่ปลอดภัยกับ production: `seedPlatform` `seedRbac` `seedCatalog` `seedForms`
  `seedBilling` — ทั้งหมดเป็น upsert ที่แตะเฉพาะตารางตั้งค่า
  ขั้นที่**ห้ามรัน**: `seedDemo` (ลูกค้า/งานตัวอย่าง) และ `seedEmployees`
- **seed ปลดระวางแถวที่หายจากลิสต์ให้อัตโนมัติ** — `seedCatalog()` ปิด `isActive` ของ
  `ServiceCatalogItem` ที่ code ไม่อยู่ในลิสต์แล้ว ถ้าเพิ่มแถวใหม่โดยไม่ลบของเก่า
  จะมี 2 แถว active ของเครื่องประเภทเดียวกัน แล้วราคาที่ลูกค้าเห็นขึ้นกับลำดับที่ DB คืนมา
- **`createJobFromIntake()` ไม่กินโควตาโดยตั้งใจ** (งานทางโทรศัพท์ให้ผู้จ่ายงานจัดคิวทีหลัง)
  การจองผ่านเว็บต้องใช้ `createJobFromBooking()` เท่านั้น — ใช้ผิดตัว = ขายคิวเกิน
- **`priceMin`/`priceMax` คือช่วงราคาที่ใช้เสนอ ไม่ใช่ราคาแยกตามประเภทลูกค้า**
  ลูกค้ายืนยัน 9 ส.ค. 2569 ว่าราคาขึ้นกับสภาพหน้างาน ความยากง่าย ความสูง และจำนวนเครื่อง
  (จำนวนมาก → ราคาต่อเครื่องลดลง) **`resolvePrice()` จึงคืนช่วงราคา ไม่คืนตัวเลขเดียว โดยตั้งใจ**
  ห้ามเขียนโค้ดที่เดาตัวเลขจากช่วงนี้ — ราคาจริงต้องมาจากการเสนอราคาของเจ้าหน้าที่
  (เดิมชื่อ `priceContract`/`priceStandard` ซึ่งอ่านผิดว่าลูกค้าในสัญญาจ่ายเลขต่ำเสมอ)
- **transaction ที่แย่งกันต้องตั้ง `maxWait`** ค่า 2 วินาทีของ Prisma สั้นกว่าคิวที่มันสร้างเอง
  ลูกค้าที่แพ้การแย่งคิวจะเห็น "ระบบขัดข้อง" แทน "วันนี้เต็ม"
- **เพิ่ม API ที่คิวออฟไลน์ยิง ต้องใส่ใน `PUBLIC_PREFIXES`** (`src/lib/auth/constants.ts`)
  ไม่ใช่เพราะเปิดให้ใครก็ได้ แต่เพราะ route ต้องได้ตอบ **401** เอง —
  ถ้าถูก middleware redirect ไปหน้า login `fetch` จะตามไปแล้วได้ **200**
  คิวจะอ่านว่า "ส่งสำเร็จ" แล้ว**ลบงานของช่างทิ้งโดยไม่มีใครรู้** (เคยเป็นบั๊กจริง)
  ตัวส่งตั้ง `redirect: 'manual'` กันไว้อีกชั้น และมีเทสต์ล็อกทั้งสองอย่าง
- **`401` ต้องนับเป็น "ลองใหม่ได้" ไม่ใช่ปฏิเสธถาวร** — session หมดอายุระหว่างงานค้างในคิว
  เป็นเรื่องปกติของการเซ็นบนหลังคาแล้วซิงก์วันรุ่งขึ้น ช่างแก้เองได้ด้วยการล็อกอินใหม่
  ส่วน `403` เป็นถาวร เพราะล็อกอินอยู่แล้วแต่ไม่มีสิทธิ์
- **ข้อมูลพนักงานอ่อนไหวกว่าข้อมูลลูกค้า** — `Employee` เก็บเลขบัตรประชาชน เลขบัญชี
  และเงินเดือนรายคน เข้ารหัสด้วย `src/lib/crypto/field.ts` (AES-256-GCM, IV สุ่มใหม่ทุกค่า)
  **การอ่านปกติไม่คืน 3 ฟิลด์นี้ทุกระดับสิทธิ์** ต้องเรียก `viewSensitive()` ซึ่ง
  **เขียนล็อกก่อนถอดรหัสและ await** — เขียนล็อกไม่ได้ = ไม่ให้ข้อมูล
  ถ้าคืนมาโดยอัตโนมัติ เดี๋ยวไปโผล่ในหน้า list ในล็อก ในแคช แล้วการเข้ารหัสก็ไร้ความหมาย
- **`FIELD_ENCRYPTION_KEY` หาย = ข้อมูล 2 คอลัมน์นั้นอ่านไม่ได้ตลอดกาล** ไม่มีทางกู้ ตั้งใจแบบนั้น
  ต้องสำรองไว้คนละที่กับฐานข้อมูล — เก็บที่เดียวกันแล้วการเข้ารหัสไม่มีความหมาย
- **ช่องเลขบัตร/เลขบัญชีที่ว่างตอนแก้ไข = "ไม่แก้ของเดิม" ไม่ใช่ "ลบ"** ฟอร์มแสดงค่าเดิมไม่ได้
  ช่องว่างจึงเป็นสภาพปกติของการกดบันทึกทั่วไป · ถ้าตีความว่าลบ ข้อมูลจะหายทุกครั้งที่แก้เบอร์โทร
- **raw SQL ให้ใช้ `src/lib/db/sql.ts` ไม่ใช่ `$queryRaw` ตรงๆ** — `COUNT`/`SUM` คืน bigint
  ซึ่ง **`JSON.stringify` throw** ส่งจาก server component ไป client แล้วหน้าขาวทั้งหน้า
  ชั้นนี้แปลงให้ + เตือน query ที่ช้าเกิน 250ms + `all()`/`inList()` กันเงื่อนไขว่างพัง
- **สีในกราฟผูกกับชื่อ ไม่ใช่ลำดับ** — ถ้าใช้ palette ตามลำดับ ประเภทงานที่เป็นศูนย์ในเดือนนั้น
  จะยกสีให้ประเภทถัดไป คนอ่านเข้าใจกราฟผิดทั้งใบ
- **อย่าเปลี่ยนกราฟคิวงานกลับไปเป็น dual-axis** สองแกนสเกลแยกกัน เส้นโควตาลอยที่ความสูงมั่ว
  แท่งชนเส้นไม่ได้แปลว่าอะไร ซึ่งคือคำถามเดียวที่กราฟนั้นมีไว้ตอบ
- **`prisma migrate dev` ใช้ไม่ได้ใน non-interactive** สร้าง migration ด้วย
  `prisma migrate diff --from-config-datasource --to-schema prisma/schema --script` แล้ว `migrate deploy`
- **อย่าอ่าน `Employee.wageRate` เพื่อคำนวณเงินเดือน** ใช้ `wageAtDate()` / `wagesAtDate()`
  จาก `src/modules/hr/wage.service.ts` เสมอ · `wageRate` คือ "ค่าแรงตอนนี้" ซึ่งไม่ใช่
  "ค่าแรงที่มีผลในงวดที่กำลังคำนวณ" ปรับค่าแรง 15 ก.ย. แล้วคำนวณเดือน ส.ค. ย้อนหลัง
  วันที่ 20 ก.ย. จะได้เรทใหม่ = **จ่ายผิด** โดยไม่มีอะไรฟ้อง
- **`wageAtDate()` คืน `null` ถ้ายังไม่เคยตั้งค่าแรง** ต้องอ่านว่า "คำนวณไม่ได้"
  **ห้ามตีความเป็นศูนย์** ไม่งั้นพนักงานที่ยังไม่ได้ตั้งเรทจะถูกคำนวณว่าได้เงิน 0
- **ลบรายการปรับค่าแรงได้เฉพาะรายการล่าสุด** รายการเก่าอาจถูกใช้คำนวณเงินที่จ่ายไปแล้ว
  การแก้ฐานของเงินที่จ่ายไปแล้วแย่กว่าการปล่อยแถวที่ดูแปลก
- **seed ถอนสิทธิ์ได้แล้ว** `seedRbac()` prune `RolePermission` ให้ตรงกับ `ROLE_PERMISSIONS`
  เดิมมันเพิ่มอย่างเดียว ลบ permission ออกจากลิสต์แล้วไม่มีผลกับ DB ที่มีอยู่ — เงียบสนิท
