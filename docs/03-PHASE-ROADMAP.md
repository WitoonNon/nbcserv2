# NBC Service — แผนงานเฟส 1 · 2 · 3

> ตรวจสอบกับโค้ดจริงเมื่อ **5 สิงหาคม 2569**
> ทุกบรรทัดที่เขียนว่า "มีแล้ว" หรือ "ยังไม่มี" ยืนยันจากไฟล์จริง ไม่ได้อ้างจากเอกสารเก่า

---

## สถานะปัจจุบัน

**เฟส 1 เสร็จครบทุกข้อ · ขึ้น production แล้ว** → https://nbcserv.vercel.app

| เฟส | สถานะ | หมายเหตุ |
|---|---|---|
| เฟส 0 | ✅ เสร็จ | schema · vertical slice |
| **เฟส 1** | **✅ เสร็จ · deploy แล้ว** | = **งวดที่ 1** · รอลูกค้าทดสอบและชำระงวดที่ 2 |
| เฟส 2 | ⛔ **บล็อก** | รอฟอร์มจริง 2 ใบ + โลโก้เวกเตอร์จากลูกค้า |
| เฟส 3 | ⬜ ยังไม่เริ่ม | ทะเบียนแอร์ · รายงาน · เชื่อมเว็บเดิม |

**การทดสอบ:** 12/12 ผ่าน กับ PostgreSQL จริง · typecheck ผ่าน
**Lighthouse:** `/login` 100 · `/booking` 91

### ค้างอยู่ 3 เรื่อง (ไม่บล็อกการส่งมอบงวดที่ 1)

| # | เรื่อง | ใครทำ | ทำไมยังไม่เสร็จ |
|---|---|---|---|
| A | 🔴 **เปลี่ยนรหัสฐานข้อมูล** | **ต้องทำเอง** | Supabase บล็อก `ALTER USER postgres` ผ่าน SQL (`supautils`: *Only superusers can alter privileged roles*) role `postgres` ไม่ใช่ superuser จึงต้องกด **Settings → Database → Reset database password** ในหน้า dashboard เท่านั้น แล้วอัปเดต `DATABASE_URL` บน Vercel ทั้ง production และ preview |
| B | 🟡 ผูกโดเมน `app.nbcgroup.co.th` | รอลูกค้า | ยังไม่ได้สิทธิ์จัดการ DNS |
| C | 🟡 ย้าย rate limit ไป Redis | ทีมงาน | ตอนนี้เก็บใน memory หายเมื่อรีสตาร์ท ยอมรับได้ที่ traffic ระดับนี้ |

---

## หลักการเรียงลำดับ

เรียงตาม **การพึ่งพากันจริง** ไม่ใช่ตามความยาก — งานที่ปลดล็อกงานอื่นได้มากที่สุดขึ้นก่อน
และงานที่ลูกค้าถามเป็นข้อแรกตอนขาย ต้องเดโมได้ก่อนงานที่ลูกค้าไม่เคยถาม

---

# เฟส 1 — ✅ เสร็จครบแล้ว (ส่งมอบงวดที่ 1)

## 1.0 · ต่อฐานข้อมูลจริง ✅

ต่อ Supabase PostgreSQL 17.6 (ap-southeast-1) เรียบร้อย

```bash
npx prisma migrate deploy   # 52 ตาราง · 27 enum · migration 4 ชุด
npm run db:seed             # 19 บริการจากราคาจริง · 20 อะไหล่ · 14 วันหยุด
                            # 6 ช่าง · 3 ทีม · 54 เครื่อง · 364 ช่องโควตา
npm test                    # 12/12 ผ่าน
```

---

## 1.1 · หน้าตั้งค่าโควตา — `/settings/quota` ✅ จุดขายหลัก

**ทำไมต้องมาก่อนหน้าจองคิว:** ปฏิทินจองคิวจะแสดงวันว่างได้ ต้องมี `QuotaDay` ก่อน
ซึ่งเกิดจาก `materialiseQuota()` ซึ่งอ่านจาก `QuotaRule` — ถ้าตั้งกฎไม่ได้ ปฏิทินก็ว่างเปล่า

**ทำไมสำคัญที่สุด:** ลูกค้าถามข้อนี้เป็นคำถามแรกตอนขาย
("กำหนดได้ไหมว่าวันหนึ่งรับได้กี่เคส งานเล็ก 3 งานใหญ่ 1–2")
ถ้าเดโมแล้วโชว์หน้าจอนี้ไม่ได้ = เสียจุดขายหลัก

### ทำแล้ว

| ส่วน | ไฟล์ |
|---|---|
| `createQuotaRule()` · `updateQuotaRule()` · `deactivateQuotaRule()` | `schedule.service.ts` |
| หน้าตารางกฎ + ฟอร์มแก้ไข (ประเภทงาน · เขต · วันในสัปดาห์ · 3 แกน · ช่วงวันที่ · ลำดับ) | `settings/quota/page.tsx` · `QuotaRuleManager.tsx` |
| ปุ่ม **คำนวณปฏิทินใหม่** → `materialiseQuota()` 90 วัน | `settings/quota/actions.ts` |
| **แก้โควตาเฉพาะวันเดียว** ที่ `/schedule` → `setDayCapacity()` | `schedule/actions.ts` · `QuotaCalendar.tsx` |

**กติกาที่ฝังไว้ในโค้ด:**
- กฎที่ไม่กำหนดสักแกนถูกปฏิเสธ — ไม่งั้นวันนั้นจะรับงานไม่จำกัดทั้งที่ดูเหมือนมีเพดาน
- กฎถูก **ปิดการใช้งาน ไม่ลบทิ้ง** — ไม่งั้นจะอธิบายไม่ได้ว่าทำไมวันในอดีตมีความจุเท่านั้น
- ลดเพดานต่ำกว่างานที่จองแล้ว **ไม่ยกเลิกงานเดิม** แค่หยุดขายเพิ่ม (งานสัญญากับลูกค้าไปแล้ว)
- ทุกการแก้รายวันเขียน `QuotaOverrideLog` พร้อมเหตุผลและผู้ทำ

### จุดที่ต้องระวังตอนอธิบายลูกค้า

ระบบ **ไม่แยกช่องตามขนาดงาน** — จงใจ ดูเหตุผลใน `scheduling.prisma:47-53`
"งานใหญ่รับได้ 1–2 เคส" เกิดจากแกน **นาทีทีมช่าง** ไม่ใช่ช่องแยก

ตัวอย่างที่ควรใช้เดโม — ตั้ง `maxTechnicianMinutes = 480`:

| งาน | คำนวณ | ผล |
|---|---|---|
| ล้างแอร์ติดผนัง 30 นาที/เครื่อง | 480 ÷ 30 | รับได้ 16 เครื่อง |
| แอร์ซ่อนในฝ้าใหญ่ 90 นาที/เครื่อง | 480 ÷ 90 | รับได้ 5 เครื่อง |
| งานโรงงาน 40 เครื่อง × 90 นาที | 3,600 > 480 | **เคสเดียวเต็มวัน** |

**คำที่ควรใช้:** "ระบบคิดจากกำลังช่างที่ใช้จริง" — ไม่ใช่ "แยกช่องเล็ก/ใหญ่"

**เกณฑ์ว่าเสร็จ:** แอดมินตั้ง "จันทร์-ศุกร์ ล้าง-PM รับ 8 งาน / 480 นาที" ได้จากหน้าเว็บ
กดคำนวณปฏิทิน แล้วหน้า `/booking` แสดงวันว่างตามนั้นทันที

---

## 1.2 · หน้าจองคิวลูกค้า — `/booking` ✅

ขั้นตอน: เลือกงาน → เลือกวัน → ยืนยันค่าตรวจเช็ค → จองสำเร็จ

| ทำแล้ว | รายละเอียด |
|---|---|
| เลือกประเภทงาน · ประเภทเครื่อง · จำนวนเครื่อง | ปฏิทินคำนวณใหม่ทุกครั้ง — งาน 40 เครื่องกับ 1 เครื่องมีวันว่างไม่เหมือนกัน |
| ปฏิทิน **แยกรายเดือน + ปุ่มเลือกเดือน** | เปิดเต็ม 90 วันตาม `QUOTA_HORIZON_DAYS` · แต่ละปุ่มบอกว่าเดือนนั้นว่างกี่วัน |
| เลือกวัน → `holdSlot()` | กันคิว 10 นาทีระหว่างกรอกข้อมูล |
| **หน้ายืนยันค่าตรวจเช็ค** | `previewInspectionFee()` · ลูกค้าในสัญญาขึ้น "ยกเว้นตามสัญญา" · **ไม่ติ๊กยอมรับ = จองไม่ได้** |
| ยืนยันจอง → `createJobFromBooking()` | เรียก `bookSlotWithin()` **ในทรานแซกชันเดียวกับการสร้าง Job** |
| หน้าสำเร็จ + ลิงก์ไปหน้าติดตาม | แสดงเลขที่งาน |

> ⚠️ **กติกาที่ห้ามพัง:** `createJobFromIntake()` **ไม่กินโควตา** โดยตั้งใจ (งานรับทางโทรศัพท์
> ให้ผู้จ่ายงานจัดคิวทีหลัง) การจองผ่านเว็บจึงมีเส้นทางของตัวเองคือ `createJobFromBooking()`
> ถ้าเผลอเอา intake มาใช้กับการจองเว็บ = ขายคิวเกิน มี `tests/booking.quota.test.ts` เฝ้าไว้แล้ว

**ทดสอบแล้ว:** จอง 10 รายการพร้อมกันแย่งคิวสุดท้าย → สำเร็จ 1 · งานใหญ่ 3,600 นาทีถูกปฏิเสธ
แม้จำนวนงานยังไม่เต็ม · การจองที่ถูกปฏิเสธไม่ทิ้ง Job ค้างไว้

---

## 1.3 · หน้าติดตามงาน — `/track` ✅

1. ✅ ค้นด้วย **เลขงาน + เบอร์โทร** — เลขงานเรียงลำดับ เดาได้ จึงต้องใช้คู่กับเบอร์
2. ✅ ไทม์ไลน์สถานะจาก `JobStatusEvent`
3. ✅ วันนัด · ทีมช่างที่รับผิดชอบ (เฉพาะที่ยังไม่ถูกถอน) · ค่าใช้จ่ายที่บันทึกแล้ว
4. 🟡 ขอเลื่อนนัด — ตอนนี้ให้โทรแจ้ง ยังไม่มีปุ่มในระบบ

ยอดเงินคำนวณจาก `SUM(amountSigned)` เสมอ ลูกค้ากับออฟฟิศจึงเห็นตัวเลขเดียวกันแน่นอน

---

## 1.4 · งานเบื้องหลัง 🟡

ตอนนี้ไม่มีอะไรรันอัตโนมัติเลย — ต้องเข้าไปกดเองทุกอย่าง

| งาน | ฟังก์ชัน | ความถี่ | ถ้าไม่ทำจะเป็นอะไร |
|---|---|---|---|
| สร้างช่องโควตาล่วงหน้า 90 วัน | `materialiseQuota()` | ทุกคืน 00:30 น. | ปฏิทินหมดอายุ ลูกค้าจองล่วงหน้าไม่ได้ |
| กวาดคิวจองที่หมดอายุ | `sweepExpiredHolds()` | ทุกคืน 01:00 น. | แค่มีแถวขยะค้างในตาราง |

> **การกวาด hold ไม่ใช่เรื่องความถูกต้อง เป็นแค่การเก็บกวาด** — ทั้ง `getAvailability()`
> และ `bookSlotWithin()` กรอง `expiresAt > NOW()` อยู่แล้ว hold ที่หมดอายุจึงหยุดกินโควตา
> ทันทีที่หมดเวลา ไม่ต้องรอถูกกวาด จึงรันวันละครั้งพอ
> (สำคัญเพราะ **Vercel Hobby รัน cron ได้วันละครั้งเท่านั้น** — ถ้าอัปเป็น Pro
> จะปรับเป็นทุก 10 นาทีก็ได้ แต่ไม่จำเป็น)

---

## 1.5 · เก็บงานความปลอดภัย ✅ / 🟡

1. ✅ **บังคับเปลี่ยนรหัสผ่านครั้งแรก** — `User.mustChangePassword` (migration `20260804173942`)
   - `requireUser()` เด้งไป `/change-password` · `assertPermission()` ปฏิเสธการเขียนด้วย
     (แท็บที่เปิดค้างไว้จึงส่งข้อมูลไม่ได้)
   - ตั้งรหัสใหม่แล้ว **เตะ session อื่นทั้งหมดออก** — รหัสเดิมคือสิ่งที่กำลังถูกเพิกถอน
   - ตั้งรหัสใหม่ซ้ำกับรหัสเดิมไม่ได้ (กันวิธีเลี่ยงที่ตรงไปตรงมาที่สุด)
   - บัญชีเดโมใน seed ยกเว้นไว้ เพราะรหัสประกาศอยู่ใน README และต้องล็อกอินซ้ำ ๆ ตอนเดโม
     บัญชีที่สร้างผ่าน `/settings/users` ติดธงเสมอ
2. 🟡 ย้ายตัวจำกัดการล็อกอินผิดไป Redis — ตอนนี้เก็บใน memory หายเมื่อรีสตาร์ท
3. 🔴 **เปลี่ยนรหัสฐานข้อมูล — ยังไม่ได้ทำ ต้องทำในหน้า Supabase dashboard**
   ลองเปลี่ยนผ่าน SQL แล้วไม่ได้ Supabase บล็อกด้วย extension `supautils`:
   ```
   ERROR: Only superusers can alter privileged roles.  (supautils_hook)
   ```
   role `postgres` บน Supabase มี `rolsuper: false` จึงแก้รหัสตัวเองผ่าน `ALTER USER` ไม่ได้
   **วิธีทำ:** Supabase → Settings → Database → Reset database password
   แล้วอัปเดต `DATABASE_URL` บน Vercel ทั้ง production และ preview (อย่าลืมใส่ `?pgbouncer=true`)

---

## 1.6 · ขึ้นเซิร์ฟเวอร์จริง ✅

1. ✅ Deploy แล้ว — **https://nbcserv.vercel.app** (`touchmecodes-projects/nbcserv`)
2. ⬜ โดเมน `app.nbcgroup.co.th` — **รอสิทธิ์จัดการ DNS จากลูกค้า**
3. ✅ ใช้ connection pooler แล้ว — จำเป็น เพราะ Supabase direct host เป็น **IPv6-only**
   (ตรวจแล้ว มีแต่ AAAA record) ซึ่ง Vercel Functions ต่อไม่ได้
4. ⬜ สำรองข้อมูลอัตโนมัติ — แผนฟรีของ Supabase ไม่มีให้

### บทเรียนจากการ deploy จริง

- **`.vercelignore` จำเป็น** — Vercel ไม่ได้ใช้ `.gitignore` ในการเลือกไฟล์อัปโหลด
  ครั้งแรก `.env` (ที่มีรหัสฐานข้อมูล) ถูกส่งขึ้น build ไปด้วย
- **ตั้ง env ต้องใช้ `vercel env add K E --value V`** — ถ้า pipe ค่าเข้าทาง stdin
  PowerShell จะเติม newline ต่อท้าย ทำให้ deploy ล้มด้วย
  *"CRON_SECRET contains leading or trailing whitespace"*
- **Vercel Hobby รัน cron ได้วันละครั้ง** — `*/10 * * * *` ทำให้ deploy ล้มทันที
- **`Error: fetch failed` ของ CLI มักไม่ใช่ build ล้มจริง** — เป็น log stream หลุด
  ให้เช็คด้วย `vercel ls` ว่าสถานะเป็น `● Ready` หรือไม่

---

## สรุปเฟส 1

| # | งาน | สถานะ |
|---|---|---|
| 1.0 | ต่อฐานข้อมูล | ✅ Supabase สิงคโปร์ · 52 ตาราง · seed ครบ |
| 1.1 | หน้าตั้งค่าโควตา | ✅ `/settings/quota` + ปรับรายวันที่ `/schedule` |
| 1.2 | หน้าจองคิวลูกค้า | ✅ จองจบจริง กินโควตาจริง · 5 เทสต์คุม |
| 1.3 | หน้าติดตามงาน | ✅ `/track` ค้นด้วยเลขงาน + เบอร์โทร |
| 1.4 | งานเบื้องหลัง | ✅ Vercel Cron 2 ตัว + bearer secret |
| 1.5 | เก็บงานความปลอดภัย | ✅ บังคับเปลี่ยนรหัสครั้งแรก · 🔴 **รหัส DB ยังไม่เปลี่ยน** · 🟡 rate limit ยังอยู่ใน memory |
| 1.6 | ขึ้นเซิร์ฟเวอร์ | ✅ https://nbcserv.vercel.app · 🟡 ยังไม่ได้ผูกโดเมนจริง |

### วัดผลจริงหลังทำเสร็จ

| | |
|---|---|
| เทสต์ | **12/12 ผ่าน** กับ Postgres จริง (โควตา 7 + การจอง 5) |
| Lighthouse `/login` | **100** · FCP 0.4s · LCP 0.5s · TBT 0ms · CLS 0 |
| Lighthouse `/booking` | **91** perf · **96** accessibility · CLS 0 |
| typecheck · build | ผ่านทั้งคู่ |

> ⚠️ **วัด Lighthouse บน `npm run dev` ไม่มีความหมาย** — dev build ไม่ minify
> มี HMR + source map เต็ม วัดได้ 50 ทั้งที่ production ได้ 100
> ต้อง `npm run build && npm run start` ก่อนวัดเสมอ

---

# เฟส 2 — งวดที่ 2 · เริ่มเมื่อลูกค้าคอนเฟิร์มฟอร์มจริง

> **ตัวปลดล็อกเฟส 2 = ฟอร์มจริงอีก 2 ใบ**
> ได้ใบซ่อมมาแล้ว (ทำเป็น v2 ตรงต้นฉบับเรียบร้อย) เหลือใบตรวจเช็ค/แจ้งซ่อม และใบล้าง/PM
> **ยิ่งได้เร็ว เฟส 2 ยิ่งเริ่มได้เร็ว** — ควรทวงตั้งแต่ตอนนี้

## 2.0 · รับฟอร์มจริง 2 ใบ

| ใบงาน | code | เวอร์ชัน | สถานะ |
|---|---|---|---|
| ใบตรวจเช็ค / แจ้งซ่อม | `INSPECTION_REQUEST` | v1 | โครงร่าง — **รอฟอร์มจริง** |
| ใบล้าง / PM | `CLEANING_PM` | v1 | โครงร่าง — **รอฟอร์มจริง** |
| ใบซ่อม | `REPAIR` | **v2** | ✅ ตรงต้นฉบับแล้ว |

ฟอร์มเก็บเป็น**ข้อมูล ไม่ใช่โค้ด** (`src/lib/forms/templates.ts`)
พอได้ฟอร์มจริงมา เพิ่มเป็น v2 ได้เลย **ไม่ต้องแก้โปรแกรม ไม่ต้อง migrate**

`templateVersion` ถูกบันทึกบนใบงานแต่ละใบ — ใบที่ออกปี 2569 จะแสดงผลแบบเดิมเสมอ
แม้ฟอร์มเปลี่ยนไปแล้วในปี 2570

**พร้อมกันนี้ควรขอ: โลโก้ไฟล์เวกเตอร์** — ที่ใช้อยู่เป็น PNG ความละเอียดต่ำจากเว็บ
พิมพ์ลง PDF แล้วไม่คมชัด **ต้องได้ก่อนทำข้อ 2.4**

## 2.1 · บันทึกใบงานลงฐานข้อมูล

ตอนนี้แสดงฟอร์มได้ แต่ปุ่มบันทึกปิดอยู่

1. `saveWorkOrder()` — เก็บคำตอบเป็น JSONB พร้อมล็อก `templateVersion`
2. สถานะใบงาน `DRAFT → SUBMITTED → APPROVED / RETURNED` (enum มีแล้ว)
3. หัวหน้างานอนุมัติ / ตีกลับ พร้อมเหตุผล

## 2.2 · แนบรูปถ่าย

`StorageAdapter` ยังเป็น stub — แนะนำ **Supabase Storage** เพราะใช้โปรเจกต์เดียวกับ DB อยู่แล้ว

1. เขียน adapter จริง (โครง local ⇄ s3 มีแล้ว ไม่ต้องออกแบบใหม่)
2. อัปโหลดจากมือถือ — ย่อรูปฝั่ง client ก่อนส่ง (ช่างใช้เน็ตมือถือ)
3. จัดหมวดรูปตาม `AttachmentKind` ที่มีแล้ว: ก่อนซ่อม · หลังซ่อม · จุดเสีย · เพลทเครื่อง · ซีเรียล
4. รูปหลายรูปต่อใบงาน

## 2.3 · ลายเซ็น

1. Canvas เซ็นด้วยนิ้วบนมือถือ
2. บันทึก `Signature.payloadHash` = SHA-256 ของเนื้อหาตอนเซ็น
   **นี่คือความต่างระหว่าง "รูปลายเซ็น" กับ "หลักฐาน"** — แก้ทีหลังจะตรวจจับได้
3. 3 บทบาท: ลูกค้า · ช่าง · หัวหน้างาน (enum `SignerRole` มีแล้ว)

## 2.4 · Export PDF ภาษาไทย 🔴 สัญญากับลูกค้าไว้แล้ว

พิสูจน์เทคนิคแล้วว่าทำได้ด้วย **Chromium + ฟอนต์ Sarabun**

1. เรนเดอร์ใบงานเป็น PDF ตรงตามฟอร์มต้นฉบับ **พร้อมปริ้นได้ทันที**
2. ฝังรูปถ่าย + ลายเซ็นลงในเอกสาร
3. ดาวน์โหลด / ส่งอีเมล / ส่งลิงก์ให้ลูกค้า
4. เก็บไฟล์ไว้เปิดย้อนหลังได้

> ตอนขายบอกลูกค้าว่า "ใบงานรวมอยู่ในฟังก์ชัน export แล้ว ราคาเท่าเดิม"
> — ข้อนี้จึงเป็น **ภาระผูกพัน ไม่ใช่ของแถม**

## 2.5 · แอปช่างภาคสนาม

1. ช่างกรอกใบงานจากมือถือได้จริง
2. **ทำงานตอนไม่มีสัญญาณ** แล้ว sync ทีหลัง (ฟอนต์โฮสต์เองไว้แล้วเพื่อข้อนี้)
3. ปุ่มเปลี่ยนสถานะ: ออกเดินทาง → ถึงหน้างาน → กำลังทำ → เสร็จ

> ⚠️ **บั๊กที่เคยเกิดจริง:** คอลัมน์ `@db.Date` เก็บเป็นเที่ยงคืน **UTC**
> ต้องใช้ `dateOnly()` เทียบเสมอ ถ้าใช้เที่ยงคืนเวลาไทยจะเลื่อนไปวันก่อนหน้าและหาไม่เจอ

## 2.6 · แจ้งเตือน LINE

รอสิทธิ์ Messaging API ของ `@nbcservice` — `NotificationChannel` เตรียมโครงไว้แล้ว
สลับจาก `console` เป็น `line` ได้โดยไม่แก้โค้ดที่เรียกใช้

---

# เฟส 3 — หลังใช้งานจริงแล้ว

## 3.1 · ทะเบียนเครื่องปรับอากาศ

`/assets` เป็นหน้าเปล่าบอกขอบเขต — schema พร้อมแล้ว (`AcType` มี 10 ประเภท)

1. ทะเบียนเครื่องรายตัว: ยี่ห้อ · รุ่น · BTU · ซีเรียล · ตำแหน่งติดตั้ง
2. ผูกประวัติการซ่อม/ล้างเข้ากับเครื่อง ไม่ใช่แค่กับลูกค้า
3. **ตอบคำถามว่า "เครื่องตัวนี้ซ่อมมากี่ครั้งแล้ว ควรเปลี่ยนหรือยัง"**
4. QR code ติดเครื่อง — ช่างสแกนแล้วเห็นประวัติทันที

## 3.2 · รายงานและสถิติ

`/reports` เป็นหน้าเปล่า — ข้อมูลดิบมีครบแล้วในฐานข้อมูล

1. รายได้แยกตามประเภทงาน / เขต / ช่วงเวลา
2. อัตราการใช้โควตา — ตั้งไว้เหมาะสมหรือยัง วันไหนเต็มบ่อย วันไหนว่างตลอด
3. ผลงานทีมช่าง — จำนวนงาน เวลาเฉลี่ย งานที่ถูกตีกลับ
4. อาการเสียที่พบบ่อย — ป้อนกลับไปที่การสต๊อกอะไหล่
5. ลูกค้าที่เรียกซ้ำบ่อย — สัญญาณว่าซ่อมไม่จบ
6. Export Excel

## 3.3 · เชื่อมกับเว็บเดิม nbcgroup.co.th

ตอนขายรับปากไว้ว่า "เชื่อมต่อกับเว็บไซต์เดิมได้ ไม่ต้องสร้างใหม่"

1. ตรวจโครงสร้างเว็บ WordPress เดิมก่อน
2. **วิธีที่แนะนำ:** ปุ่ม "จองคิวช่าง" บนเว็บเดิม → ลิงก์มาที่ `app.nbcgroup.co.th/booking`
   (ไม่แตะโค้ดเว็บเดิมเลย ปลอดภัยที่สุด)
3. ทำธีมให้กลมกลืน — โทเคนสีจากเว็บบริษัทใส่ไว้แล้ว
   ส้ม `#E4750E` · น้ำเงิน `#2891BD` · กรมท่า `#132945`

## 3.4 · นัด PM อัตโนมัติ

ตารางราคามีรอบ PM 2/3/4 ครั้งต่อปีตามลักษณะการใช้งานอยู่แล้ว

1. ถึงรอบ → สร้างงานอัตโนมัติ เข้าคิวรอยืนยัน
2. แจ้งเตือนลูกค้าล่วงหน้า
3. **กระจายงาน PM ไม่ให้กระจุกวันเดียวกัน** — ต้องเช็คโควตาก่อนสร้าง

## 3.5 · ดูแลระบบระยะยาว

1. สำรองข้อมูลอัตโนมัติ + ทดสอบกู้คืนจริง
2. Monitoring + แจ้งเตือนเมื่อระบบล่ม
3. **วันหยุดนักขัตฤกษ์แบบจันทรคติ** (มาฆบูชา วิสาขบูชา อาสาฬหบูชา เข้าพรรษา)
   ระบบใส่เฉพาะวันหยุดที่วันที่ตายตัว **ไม่เดาวันจันทรคติ** เพราะใส่ผิด
   จะปิดรับงานในวันทำงานโดยไม่มีใครรู้ — ต้องให้ลูกค้ายืนยันรายปี

---

# รอลูกค้า — ไม่บล็อกงานข้างบน แต่ควรทวง

| สิ่งที่รอ | บล็อกอะไร | ความเร่งด่วน |
|---|---|---|
| **ฟอร์มจริง 2 ใบ** (ตรวจเช็ค/แจ้งซ่อม · ล้าง/PM) | เฟส 2 ทั้งเฟส | 🔴 สูงสุด |
| **โลโก้ไฟล์เวกเตอร์** | PDF ข้อ 2.4 พิมพ์ออกมาไม่คม | 🔴 สูง |
| สิทธิ์ LINE Messaging API `@nbcservice` | ข้อ 2.6 | 🟡 |
| สิทธิ์จัดการ DNS | ข้อ 1.6 โดเมนจริง | 🟡 |
| ตารางราคาฉบับใช้จริงภายใน | ตอนนี้ใช้ราคาหน้าเว็บ ซึ่งใช้เดโมได้ | 🟢 |
| รายการอะไหล่จริง | ข้อ 3.2 สถิติอะไหล่ | 🟢 |
| วันหยุดนักขัตฤกษ์จันทรคติ | ข้อ 3.5 | 🟢 |

ดูรายการค่าที่ยังรอยืนยันทั้งหมด: `npm run client-confirm`

---

# สิ่งที่ผู้พัฒนาต่อต้องรู้

- **ค่าที่ลูกค้ายังไม่ยืนยัน เก็บเป็นข้อมูล ไม่ฝังในโค้ด** — ตาราง `AppConfig`
  และหน้า `/settings/assumptions` แก้ได้โดยไม่ต้องแก้โปรแกรม
- **ชื่อตารางเป็น snake_case แต่ชื่อคอลัมน์เป็น camelCase** — SQL ดิบต้องใส่ `"quotes"` ครอบชื่อคอลัมน์
- **`@db.Date` เก็บเป็นเที่ยงคืน UTC** — ใช้ `dateOnly()` เทียบเสมอ
- **`middleware.ts` รันบน Edge runtime** — ห้าม import `node:crypto` หรือ Prisma
- **CSS ที่ต้องชนะ Tailwind ต้องอยู่นอก `@layer`**
- **ห้ามแยกโควตาตามขนาดงาน** — ดูเหตุผลใน `scheduling.prisma:47-53`
  ถ้าแยก วันหนึ่งจะขึ้นว่า "ว่าง" สำหรับงานเล็ก ทั้งที่ช่างถูกจองหมดแล้ว
- **การจองต้องกินโควตาเสมอ** — `createJobFromIntake()` ไม่กินโควตาโดยตั้งใจ
  ห้ามเอาไปใช้กับการจองผ่านเว็บ ใช้ `createJobFromBooking()` แทน
  (`tests/booking.quota.test.ts` คุมไว้แล้ว)
- **`where: { field: undefined }` ใน Prisma แปลว่า "ไม่กรองเลย" ไม่ใช่ "ไม่ตรงอะไร"**
  เทสต์ที่ teardown ด้วย `deleteMany({ where: { zoneId } })` โดย `zoneId` ยังไม่ถูกเซ็ต
  จะลบ **ทุกแถวในตาราง** — เคยเกิดจริง ปฏิทินโควตาหายทั้งตาราง
  ตอนนี้ teardown ทั้งสองไฟล์มี `if (zoneId)` ครอบแล้ว
- **`npm run dev` วัด performance ไม่ได้** — ต้อง `npm run build && npm run start`
